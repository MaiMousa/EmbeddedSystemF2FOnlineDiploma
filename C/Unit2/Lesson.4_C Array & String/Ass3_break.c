/*
 * Ass3_break.c
 *
 *  Created on: Aug 31, 2020
 *      Author: Mai Mousa
 */

/*
#include <stdio.h>

int main (void)
{
	int max, i;
	float num, average, sum = 0;

	printf("Maximum no. of inputs\n");
	fflush(stdin);     fflush(stdout);
	scanf("%d", &max);

	for (i = 1; i <= max; i++)
	{
		printf("Enter n%d: ", i);
		fflush(stdin);		fflush(stdout);
		scanf("%f", &num);

		if (num < 0)	break;
		sum += num;
	}

	average = sum / (i - 1);
	printf("Average = %.2f\n", average);

	return 0;
}
*/


